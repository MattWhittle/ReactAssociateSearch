const React = require('react');

const SearchApp = React.createClass({
    render: function () {
        return (
            <div>
            SearchApp
            </div>
        )
    }
});

module.exports = SearchApp;